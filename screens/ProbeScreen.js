import React, {useState} from 'react';
import {StyleSheet, View} from 'react-native';

export default function ProbeScreen({navigation}) {
    return(
        <View style={styles.mainContainer}>
        </View>
    );
}

const styles = StyleSheet.create({
    mainContainer: {
        flex: 1,
    },
});
